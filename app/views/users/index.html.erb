<p id="notice"><%= notice %></p>

<h1>Foodie List</h1></br>

<%= link_to "Home", root_path %> |
<%= link_to "Food Log List", posts_path %> |
<%= link_to "Record New Log", new_post_path, class: "new_post" %></br>

</br>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Bio</th>
      <th>Email</th>
      <th colspan="5"></th>
    </tr>
  </thead></br>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.bio %></td>
        <td><%= user.email %></td>
        <td><%= user.password %></td>
        <td><%= user.password_confirmation %></td>
        <td><%= link_to 'Show Profile', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
