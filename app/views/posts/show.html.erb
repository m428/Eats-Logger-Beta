<h1> Here's That Food Log </h1>

<%= image_tag @post.image.url %>
<h4><%= @post.caption %></p></h4>



<h6> <%= @post.comments.size %> comments </h6>
<!-- Embed new comment form in article show action. Comment on article page directly. -->
<p><%= render partial: 'posts/comment', collection: @post.comments %>
<%= render partial: 'comments/form' %>
</p></br>

<%= link_to "Home", root_path %> |
<%= link_to "Food Log List", posts_path %> |
<% if logged_in? %>
  <%= link_to "Edit Post", edit_post_path(@post) %> |
  <%= link_to "Remove Post", post_path(@post), method: :delete, data: {confirm: "Are you sure?"} %> |
  <%= link_to "Record New Log", new_post_path, class: "new_post" %>
<% end %>
